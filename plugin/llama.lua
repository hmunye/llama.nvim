-- Files in the `plugin` directory are immediately executed on startup of Neovim

-- Files in the `lua` directory are not immediately executed on startup of Neovim
-- but are available to the user to `require`
